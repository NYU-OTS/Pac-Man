export const KEY_PRESS = 'KEY_PRESS';
export type KEY_PRESS = typeof KEY_PRESS;

export const START_GAME = 'START_GAME';
export type START_GAME = typeof START_GAME;

export enum Status{
    YetToStart,
    InProgress,
    Over,
}
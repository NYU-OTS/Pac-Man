// In Progress
export class Astar {
    public init(grid: number[][]) {
        for(let x = 0; grid.length; x++) {
            for( let y = 0; grid[x].length; y++)
            {
                // grid[x][y].f = "";
            }
        }
    }
}
